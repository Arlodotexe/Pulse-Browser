<Page
    x:Class="Pulse_Browser.MainShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pulse_Browser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:usercontrols="using:Pulse_Browser.UserControls"
    xmlns:helpers="using:Pulse_Browser.Helpers"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <usercontrols:NavigationBar BackButtonEnabled="{x:Bind AppWebView.CanGoBack, Mode=OneWay}" ForwardButtonEnabled="{x:Bind AppWebView.CanGoForward, Mode=OneWay}" />

        <Grid Grid.Row="1">
            <Frame Loaded="AppFrame_Loaded" x:Name="AppFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <WebView x:Name="AppWebView" Visibility="{x:Bind helpers:Converters.BooleanToVisibility(ViewModel.WebViewShown), Mode=OneWay}" Source="{x:Bind ViewModel.CurrentAddress, Mode=TwoWay}" />
        </Grid>
    </Grid>
</Page>
