<UserControl
    x:Class="Pulse_Browser.UserControls.Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pulse_Browser.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="700"
    d:DesignWidth="550" Height="700">

    <StackPanel>
        <Pivot Title="Options">
            <Pivot.TitleTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="28" Margin="0,0,0,-15" FontWeight="SemiLight" />
                </DataTemplate>
            </Pivot.TitleTemplate>
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="20" />
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <PivotItem Header="History">
                <local:History />
            </PivotItem>
            <PivotItem Header="Settings">
    
            </PivotItem>
        </Pivot>
    </StackPanel>
</UserControl>
